name: auth_firebase
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  #flutter_localizations:
  # sdk: flutter
  intl: ^0.19.0
  cupertino_icons: ^1.0.2
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.5.1
  qr_flutter: ^4.1.0
  logger: ^2.5.0
  local_auth: ^2.1.3
  flutter_secure_storage: ^9.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

dependency_overrides:
  async: ^2.12.0
  boolean_selector: ^2.1.2
  characters: ^1.4.0
  term_glyph: ^1.2.2
  clock: ^1.1.2
  collection: ^1.19.1
  fake_async: ^1.3.2
  http_parser: ^4.1.1
  leak_tracker: ^10.0.8
  leak_tracker_flutter_testing: ^3.0.9
  matcher: ^0.12.17
  material_color_utilities: ^0.12.0
  meta: ^1.16.0
  path: ^1.9.1
  source_span: ^1.10.1
  stack_trace: ^1.12.0
  string_scanner: ^1.4.0
  test_api: ^0.7.4
  vm_service: ^15.0.0
  lints: ^5.1.0
  stream_channel: ^2.1.3

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png
    - assets/diseño/ # Asegúrate de que esta línea esté correctamente indentada y que la carpeta exista
  